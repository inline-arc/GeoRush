# Taste
- Works on a Windows machine where the shell is cmd — avoid bash-isms like heredocs (`<<EOF`) and `;` chaining in shell commands; write multi-line content to files instead. Confidence: 0.85
- GitHub CLI (`gh`) is not installed on the user's machine; don't assume it for GitHub operations. Confidence: 0.8
- Delegates local git operations (committing and pushing changes) to the agent, but prefers to perform GitHub account-level actions (forks, pull requests) manually on github.com using agent-prepared materials (exact JSON entry, PR title, PR body) rather than pasting access tokens or installing auth tooling. Confidence: 0.7
- Expects secrets (API keys, RPC endpoints) kept in untracked env vars and never committed; values proactive detection and remediation of committed secrets (untrack + gitignore) plus a reminder to rotate anything already exposed in git history. Confidence: 0.7
- Current project is "GeoRush": an Expo React Native + TypeScript app on Starknet (map-based airdrop console; Privy wallet, react-native-maps, lucide-react-native icons) with public repo https://github.com/inline-arc/GeoRush; entered in the STRK20 Private Sprint with a planned STRK20 privacy integration (shielded funding/transfers via the Starknet Wallet API route). Confidence: 0.9
- Hands over tasks as detailed, numbered step-by-step briefs with exact commands, file contents, and URLs; expects the agent to autonomously execute every step it can and ask questions only for genuine blockers (credentials, missing personal info), then deliver a clear summary of done vs. blocked. Confidence: 0.6
- Breaks multi-step tasks into a todo list (`todo_write`) with numbered steps and tracks progress through `in_progress`/`pending` states. Confidence: 0.8
- When cmd.exe (`shell_command`) mangles output or lacks features (e.g., no `tail`), switches to the dedicated `powershell` tool for reliable command execution. Confidence: 0.85
- Wants documentation (READMEs, specs, etc.) to be detailed and properly structured — not brief or ad-hoc — with clear sections, tables, and examples. Confidence: 0.7
- Wants code and documentation to adhere to the target platform's submission rules and conventions (e.g., Starknet contract ABI/events/build config, submission calldata order, network addresses) rather than following generic patterns. Confidence: 0.7
- In Cairo code, prefers single-item imports without braces (`use starknet::get_caller_address;`) over the braced form (`use starknet::{get_caller_address};`). Confidence: 0.8
- In Cairo code, prefers using imported dispatcher types directly (`IERC20Dispatcher { ... }`) rather than fully-qualified paths (`openzeppelin::interfaces::token::erc20::IERC20Dispatcher { ... }`). Confidence: 0.75
- Uses `snforge` (`snforge test` + `snforge_std` dev dependency) as the standard test framework for Cairo/Starknet contracts. Confidence: 0.7
